# Copy this file to .env for local backend development (without Docker).
# When running via docker-compose, vars are injected from the root .env.

# PostgreSQL connection string.
DATABASE_URL=postgresql://trustlayer:trustlayer@localhost:5432/trustlayer
