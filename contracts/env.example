# Copy this file to .env for local Hardhat development (without Docker).
# When running via docker-compose, vars are injected from the root .env.

# Wallet private key (without 0x prefix) used for deploying contracts.
PRIVATE_KEY=

# Override default Monad RPC endpoints (optional).
MONAD_TESTNET_RPC=https://testnet-rpc.monad.xyz
MONAD_MAINNET_RPC=https://rpc.monad.xyz

# API key for contract verification on Monadscan (optional).
MONAD_API_KEY=
